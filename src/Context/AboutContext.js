import { createContext } from 'react';

export const AboutContext = createContext({
    "id": "default",
    "description": "default",
    "introDescription": "default context",
    "images": [],
    "details": [],
});
